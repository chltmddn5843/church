import { createSermon, deleteSermon } from "@/app/actions/sermons"
import { getSermons } from "@/lib/queries"
import { Button } from "@/components/ui/button"

export default async function AdminSermonsPage() {
  const items = await getSermons()
  return <><h1 className="text-3xl font-bold">설교 관리</h1><p className="mt-2 text-muted-foreground">YouTube 전체 링크 또는 영상 ID를 입력하면 자동으로 임베드됩니다.</p><form action={createSermon} className="mt-6 grid gap-4 rounded-2xl border bg-card p-6 shadow-sm md:grid-cols-2"><input name="title" required placeholder="설교 제목" className="h-11 rounded-lg border px-3 md:col-span-2"/><input name="preacher" placeholder="설교자" defaultValue="양승철" className="h-11 rounded-lg border px-3"/><input name="scripture" placeholder="성경 본문" className="h-11 rounded-lg border px-3"/><select name="category" className="h-11 rounded-lg border px-3"><option>주일예배</option><option>수요예배</option><option>금요예배</option><option>새벽기도</option><option>특별예배</option></select><input name="preachedAt" type="date" className="h-11 rounded-lg border px-3"/><input name="youtubeId" required placeholder="https://www.youtube.com/watch?v=..." className="h-11 rounded-lg border px-3 md:col-span-2"/><textarea name="summary" rows={5} placeholder="말씀 요약" className="rounded-lg border p-3 md:col-span-2"/><Button className="w-fit">설교 등록</Button></form><div className="mt-8 space-y-3">{items.map(item=><div key={item.id} className="flex items-center justify-between rounded-xl border bg-card p-4"><div><p className="font-semibold">{item.title}</p><p className="text-sm text-muted-foreground">{item.category} · {item.preacher}</p></div><form action={deleteSermon.bind(null,item.id)}><Button size="sm" variant="destructive">삭제</Button></form></div>)}</div></>
}
